name: not-compatible-engine
summary: This summary is too long and should trigger a validation error because it exceeds the maximum allowed characters.
vendor: Canonical Ltd
experimental: false
devices:
  allof:
    - type: cpu
      architecture: amd64
      flags: [nonexistent_flag]
